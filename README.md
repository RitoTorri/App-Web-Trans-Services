# App-Web-Trans-Services

Frontend para el sistema Trans-Services.

## 📋 Requisitos Previos

Asegúrate de tener instalado **Node.js** y **npm** en tu equipo.

## ⚙️ Configuración del Entorno

> **⚠️ Nota Importante:** Esta aplicación está configurada por defecto para conectarse a una API (Backend) que se ejecuta en el puerto **3000**.
>
> Asegúrate de que tu servidor backend esté corriendo en `http://localhost:3000` para que la aplicación funcione correctamente y pueda realizar las peticiones de datos.

## 🚀 Guía de Instalación y Ejecución

Sigue estos pasos secuenciales para levantar el proyecto en tu entorno local:

### 1. Clonar el repositorio
Clona el proyecto desde el repositorio remoto a tu máquina:

```bash
git clone https://github.com/RitoTorri/App-Web-Trans-Services.git
```

### 2. Acceder al directorio
Es necesario entrar en la carpeta específica del proyecto (Proyecto-4) antes de continuar:
```bash
cd Proyecto-4
```

### 3. Instalar dependencias
Instala todas las librerías y dependencias necesarias:
```bash
npm install
```

### 4. Iniciar el servidor de desarrollo
Ejecuta el siguiente comando para levantar la interfaz web:
```bash
npm run dev
```
Una vez ejecutado, la terminal te mostrará la URL local (generalmente http://localhost:5173/) donde podrás ver y utilizar la aplicación.